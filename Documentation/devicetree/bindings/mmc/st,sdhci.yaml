---
$id: http://devicetree.org/schemas/mmc/st,sdhci.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: STMicroelectronics sdhci-st MMC/SD controller

maintainers:
  - Lee Jones <lee@kernel.org>
  - Marco Franchi <marco.franchi@nxp.com>
  - Masanari Iida <standby24x7@gmail.com>
  - Mauro Carvalho Chehab <mchehab+samsung@kernel.org>
  - Peter Griffin <peter.griffin@linaro.org>
  - Simon Horman <horms+renesas@verge.net.au>

allOf:
  - $ref: sdhci-common.yaml#

properties:
  compatible:
    items:
      - const: st,sdhci-stih407
      - const: st,sdhci

  reg:
    items:
      - description: host controller base address
      - description: FlashSS Top register for TX/RX delay used by the driver to configure DLL inside FlashSS

  reg-names:
    items:
      - const: mmc
      - const: top-mmc-delay

  interrupts:
    maxItems: 1

  interrupt-names:
    items:
      - const: mmcirq

  clocks:
    items:
      - description: MMC bus clock
      - description: internal clock (icn)

  clock-names:
    items:
      - const: mmc
      - const: icn

  resets:
    maxItems: 1

  max-frequency:
    allOf:
      - $ref: /schemas/types.yaml#/definitions/uint32
    description: maximum bus frequency
    enum: [200000000, 100000000, 50000000]

required:
  - compatible
  - reg
  - interrupts
  - interrupt-names
  - clocks
  - clock-names

unevaluatedProperties: false

examples:
  - |
    mmc@9060000 {
        compatible = "st,sdhci-stih407", "st,sdhci";
        reg = <0x09060000 0x7ff>, <0x9061008 0x20>;
        reg-names = "mmc", "top-mmc-delay";
        interrupts = <GIC_SPI 92 IRQ_TYPE_NONE>;
        interrupt-names = "mmcirq";
        clock-names = "mmc";
        clocks = <&clk_s_c0_flexgen CLK_MMC_0>;
        vqmmc-supply = <&vmmc_reg>;
        max-frequency = <200000000>;
        bus-width = <8>;
        non-removable;
        sd-uhs-sdr50;
        sd-uhs-sdr104;
        sd-uhs-ddr50;
    };
